/*
Game.prototype._moveObjects = function () {
    player.move();
}

Game.prototype._render = function () {
    canvas.clear();

    this._drawVisibleElements();
    this._drawForeground();
    this._drawLights();

    user.drawTooltip();
}

Game.prototype._drawVisibleElements = function () {
    //ctx.globalCompositeOperation = 'source-atop';
    objects.draw(ctx);
    opponents.draw();
    player.draw();
    //bullets.draw(ctx);
    //ctx.globalCompositeOperation = 'source-over';
}

Game.prototype._drawLights = function () {
    player.drawLights();
    objects.drawLights();
}
*/
